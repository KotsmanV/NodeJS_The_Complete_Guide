<div class="centered">
    <h1>{{product.title}}</h1>
    <hr>
    <div>
        <img src="{{product.imageUrl}}" alt="book cover image">
    </div>
    <h2>{{product.price}}</h2>
    <p>{{product.description}}</p>
    {{> cart_button productId=product.id}}
    {{!-- <form action="/cart" method="post">
        <button class="btn" type="submit">Add to cart</button>
        <input type="hidden" name="productId" value="{{product.id}}">
    </form> --}}
</div>